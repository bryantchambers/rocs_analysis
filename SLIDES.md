  We have now finished a good chunk of analysis. I believe that the PCS are not being dragged to cluster around these few slow taxa samples We need to generate a polished Quarto RevealJS slide deck that guides fellow researchers
  through the analysis, grounded in the actual code and actual generated outputs.

  First inspect the repository. Focus first on our `InputQC/` sub directory and our `networkQC/` subdirectory. Do not assume the workflow from filenames alone. Read the scripts, guides, config files, result tables, logs, and available figures. 
  
  It would be best to go through:
    1. All `*.md` at the base and in each sub directory and analysis folders
    2. note our report summaries and what they tell us
    3. for bigger key results like our parameter sweep or our changes to wgcna from the original version look at the output `.tsv` and scripts to summarize
  Identify: a summary of:
  - Each analyses objectives
  - the intended run order
  - the main scripts and what each produces
  - important methodological points students and colleagues must understand
  - current output metrics/tables/figures generated by the pipeline

  The slide deck should be comprehensive but succinct, around 30 slides including the title. It should work as both a guide and a clear gounded summary of work.

  Use Quarto RevealJS. Create a `Slides/` folder if needed and generate:
  - a `.qmd` slide source
  - a small custom `styles.css`
  - rendered `.html`
  - rendered `.pdf` if Chromium/decktape is available

  Use this overall slide structure:
  1. Introduction, 3-4 slides:
     - What is the goal and purpose of the analysis in:
       1. `InputQC/`
       2. `networkQC/`
       3. main scripts in `scripts/` from input to wgcna and wgcna bootstrapping logic to the first part of the HMM scripts
     - repository and project layout. Feel free to render this as a mermaid diagram as a DAG or any other workflow figure that can be converted to a png and included in the slides. If you need additional tools please let me know or just make due with what you have. The more professional the better, but to a point
  2. Workflow overview, 1-2 slides:
     - exact scientific processes run and logic
     - key outputs to inspect
     - major error types or methodological risks the workflow handles
     - include explicit code boxes with important functions and line-level references where useful
     - be comprehensive but also concise
  3. Analysis guide:
     - 3-5 slides per major script
     - explain key scientific process, inputs, outputs, and what is happening qualitatively and mathematically
     - include actual output tables from `results/` where possible
     - embed figures from `figures/`
     - include workflow or process boxes showing the important snippets peers need to understand: e.g., wgcna process including TOM - Adj matrix and bootstrapping logic

An outline should follow roughly these major script groups:
1. `InputQC/`
2. General WGCNA logic and need for QC metrics: `scripts/` focusing on `02_wgcna.R`and `02b_wgcna_stability.R` 
	- focuse on key changes in WGCNA script and the development and deployment of the bootstrapping approach and how this is build. 
	- how we characterized the stable modules and what features were used: e.g., jaccard, percentile etc.
3. `network QC/`
	- examine more on the 
	- Overview of hyperparameter sweep and tuning for optimized parameters and stability of key test cases including exp3 exp4 opt5 etc. compared against baseline
	- summarize key modules and hyperparameters to use for final run (i believe this is exp3 with exp5 and opt5 as a more conservative [small modules limit] choice)
4. Full review of optimized outcome:
	1. `InputQC/core_age_imbalance`
	2. no sign of rarefaction issues a little bit of sample imbalance `InputQC/rarefaction_depth_qc`

  Slide design requirements:
  - Use compact, readable slide typography. Avoid huge headings that overflow.
  - Use a custom CSS file to constrain heading sizes, table sizes, code blocks, image heights, and callout boxes.
  - Use code boxes often, but keep them short.
  - Use actual generated outputs. Do not fabricate numbers.
  - Use tables for key metrics and expected outputs.
  - Use figures directly from the repo using relative paths.
  - Use callout boxes for key methodological points, warnings, and takeaways.
  - Keep bullets concise and novice-friendly.
  - Use 2-column layouts where helpful for figure + explanation or code + interpretation.
  - Make the deck visually consistent with a clean Quarto RevealJS teaching style.
  - Do not overstuff slides. Split content when needed.

  Suggested CSS style:
  - Reveal base font about 30px
  - H1 about 1.9em
  - H2 about 1.45em
  - code blocks about 0.55em
  - tables about 0.62em
  - image max-height around 500px
  - define `.small`, `.smaller`, `.caption`, `.callout`, `.warn`, `.danger`, and `.ok` classes
  - use restrained colors for callouts, not decorative backgrounds

  Example YAML:
  ---
  title: "Practical Title"
  subtitle: "A guided lab using actual workflow outputs"
  author: "Course Practical"
  date: "YYYY-MM-DD"
  format:
    revealjs:
      theme: simple
      css: styles.css
      width: 1280
      height: 720
      margin: 0.06
      slide-number: c/t
      transition: none
      controls: true
      progress: true
      self-contained: false
  execute:
    echo: false
  ---

  Important process:
  1. Inspect the repo with `rg`, `rg --files`, logs, CSVs, and script reads.
  2. Extract exact metrics from generated result files.
  3. Identify and embed key figures.
  4. Draft the `.qmd` and `styles.css`.
  5. Render with Quarto:
     `quarto render Slides/<deck>.qmd`
     or, if Quarto is only available in mamba:
     `mamba run -n <env-name> quarto render Slides/<deck>.qmd`
  6. If available, render or export PDF using Quarto/decktape/Chromium.
  7. Verify:
     - deck renders without errors
     - all image paths exist
     - slide count is around 20-25
     - no stale absolute paths remain
     - metrics match the current output files
     - headings, code blocks, tables, and figures are likely to fit
  8. Report exactly what files were created and what verification passed.

  Tone and content expectations:
  - The slides should teach colleagues both the theory and how the analysis was run not just describe it.
  - Every major claim about results should be grounded in local files with key data shown from key tables and figures
  - The deck should be domain-aware and novice-friendly.
  - Keep the practical’s scientific question visible throughout.
  - Emphasize methodological correctness, reproducibility, and how to recognize bad analysis signatures.
